include schedules
extends layout
block content
  #kurs
    #timeplan
      .content
        h1 Timeplan Vinter/Vår 2014
        each day in schedule.fields.days
          .halfbox
            mixin day(day)
    #about
      h1= courses.title
      | !{course_overview.fields.text}
      .registration= courses.register
    #levels
      .content
        h1= courses.levels
        p.main-overview!= level_overview.fields.text
      .content.wider
        each level in levels.fields.levels
          .halfbox
            h2!= level.fields.name
            | !{level.fields.desc}

